[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "scpn-fusion-rs"
version = "3.9.11"
description = "SCPN Fusion Core - Rust-accelerated plasma physics"
requires-python = ">=3.10"

[tool.maturin]
features = ["pyo3/extension-module"]
