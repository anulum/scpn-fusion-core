Relabel the disruption-mitigation and disturbance results honestly

RESULTS.md advertised "Disruption prevention rate (SNN) 100.0%". The episode behind
that number (disruption_contracts.run_disruption_episode) runs a Q-learning rl_agent on
a SYNTHETIC signal (synthetic_disruption_signal) with a heuristic risk_after blend --
it is not the SNN, not a real plasma, and "prevention" is threshold arithmetic on a
synthetic risk score. The disturbance-rejection table also shows the SNN losing to
PID/MPC by orders of magnitude in ISE while the framing implied competitiveness.

Fix the generator (collect_results.py, the source of RESULTS.md):
- Relabel the row to "Disruption mitigation rate (synthetic RL episodes)" with the note
  "synthetic-signal RL/heuristic ensemble; not SNN, not real plasma".
- Add a note under the disturbance table stating the SNN ISE is orders of magnitude
  worse than PID/MPC (stable-but-far-worse, not competitive; MPC wins every scenario).

Also surgically correct the same two labels in the committed RESULTS.md snapshot so the
false claim is gone now. This is a label-only correction (no measured number, wall-clock,
or host-env field touched); RESULTS.md is auto-generated, so it was NOT regenerated
locally (that would leak host env -- the K17-D2 evidence-drift rule). On the next
legitimate in-CI regeneration the fixed generator reproduces these labels.

No test pins the old label; stress-campaign RESULTS.md tests pass (30). validation/ is
outside the coverage gate.

Addresses KIMI due-diligence finding K17-B5 ("100% disruption prevention (SNN)") and the
disturbance-table framing under K17-B1.

Seat: 14753

Authored by Anulum Fortis & Arcane Sapience (protoscience@anulum.li)
