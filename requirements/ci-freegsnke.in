# Dedicated hash-locked FreeGSNKE inverse-parity dependencies.
# Regenerate: uv pip compile requirements/ci-freegsnke.in --generate-hashes --python-platform linux --python-version 3.12 -o requirements/ci-freegsnke.txt
freegsnke[freegs4e]==3.0.1
# FreeGSNKE 3.0.1 declares ~=0.13, which also admits the incompatible
# FreeGS4E 0.14.0 release.  Pin the last 0.13 line exercised by its baseline.
freegs4e==0.13.1
jax[cpu]==0.7.1
pytest==9.1.1

# SCPN Fusion runtime dependencies used when importing the public JAX surface.
click>=8.1,<9
pydantic>=2.0,<3
requests>=2.31,<3
